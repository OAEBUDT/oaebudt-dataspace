server:
  port: ${SERVER_PORT:8081}

spring:
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: ${ISSUER_URI:http://localhost:8080/realms/myrealm}
          jwk-set-uri: ${JWK_SET_URI:http://localhost:8080/realms/myrealm/protocol/openid-connect/certs}